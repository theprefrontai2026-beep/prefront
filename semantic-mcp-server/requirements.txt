# Prefront — Semantic MCP Server (runtime POC)
psycopg[binary]>=3.2   # Postgres access
# <2 is deliberate: mcp 2.0 removed the low-level `Server.list_tools()` /
# `call_tool()` decorators this server is built on (AttributeError at startup).
mcp>=1.24,<2           # MCP server (stdio + SSE/HTTP)
pyyaml>=6.0            # read query_templates.yaml
starlette>=0.37        # HTTP transport app
uvicorn>=0.29          # ASGI server for --http mode

# --- optional: OpenTelemetry / Arize Phoenix tracing ------------------------
# Inert unless PHOENIX_COLLECTOR_ENDPOINT is set (see tracing/prefront_tracing.py).
arize-phoenix-otel>=0.6
openinference-instrumentation-mcp>=1.1
