# Python
.venv/
__pycache__/
*.pyc

# Local state / databases
*.db

# Generated Skill Builder output (artifacts produced by pipeline runs)
skills/
skill-builder/skills/
skill-builder-data/

# Generated Semantic Layer output (Core 6 YAML artifacts)
semantic-layer/out/

# docker env (contains LLM keys)
.env

# Claude Code local settings (may contain secrets)
.claude/

# Node / Vite build output
node_modules/
dist/

# Editor swap/backup files (a transient Vim .swp was once committed by `git add -A`)
*.swp
*.swo
*~
.*.sw[a-p]

# Separate rule-extraction bake-off working dir — its own branch
# (rule-extraction-eval), not this one. 216MB of run caches; swept in here once
# by an over-broad `git add -A` and it should not happen again.
rule-extract-eval/
